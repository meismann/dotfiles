cd $HOME/Music
rm -fr /tmp/Music
mkdir /tmp/Music

for dir in *
do
  case $dir in
    *@)
      ln -s "$(pwd)/$dir" "/tmp/Music/$dir";
    ;;
  esac
done

rsync --delete --force -Lrtv -e "ssh -p 2222" /tmp/Music/ root@android-c701ac9649fb29ee:/storage/emulated/0/Music/;
